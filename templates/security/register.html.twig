{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
    <div class="col-6">
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-secondary btn-lg btn-block">Регестрация</button>
    {{ form_end(form) }}
    </div>
{% endblock %}

